import { css } from 'lit';

const styles = css`
:host {
  background-color: var(--pf-color-theme-common-background-primary);
  color: var(--pf-color-theme-common-text-primary);
  display: block;
  height: 100vh;
  margin: 0;
  padding: 0;
  width: 100vw;
}
`;

export default styles;
